info:
  name: Login
  type: http
  seq: 3

http:
  method: POST
  url: https://localhost:6001/account/login
  body:
    type: json
    data: |-
      {
        "email" :"admin@api.be",
        "password":"Password@1"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
