info:
  name: getSchedule
  type: http
  seq: 2

http:
  method: GET
  url: https://localhost:6001/schedules?shopId=6
  params:
    - name: shopId
      value: "6"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
