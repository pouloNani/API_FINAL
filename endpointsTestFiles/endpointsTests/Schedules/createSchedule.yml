info:
  name: createSchedule
  type: http
  seq: 1

http:
  method: POST
  url: https://localhost:6001/schedules
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
