info:
  name: Chat
  type: http
  seq: 1

http:
  method: POST
  url: https://localhost:6001/assistant/chat
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
